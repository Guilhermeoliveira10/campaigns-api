{
  "name": "campaign-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: No test specified\" && exit 1",
    "start": "node index.js"  // Certifique-se de ter o comando start definido
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
